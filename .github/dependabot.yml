# Keeps the actions used by the workflows in this directory current. The repo has no other
# dependency ecosystem: the tools themselves are installed by Homebrew and pinned nowhere here.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
