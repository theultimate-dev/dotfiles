-- Yazi init — The Ultimate Dev / dotfiles (public). See docs/yazi.md.
-- Git status marks, from the official git plugin restored by setup.sh.
-- `order` places the mark after the file name in the line mode.
require("git"):setup { order = 1500 }
